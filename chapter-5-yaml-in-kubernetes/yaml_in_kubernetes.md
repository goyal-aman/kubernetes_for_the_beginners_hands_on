**YAML  in Kubernetes**

1. We have create file `./resources/pod.yaml` 
2. To create pod using above file run command 
```
kubectl create -f ./resources/pod.yaml
```
3. `kubectl get pods` to confirm its created